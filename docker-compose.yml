services:
  jarvis-redis:
    image: redis:alpine
    container_name: jarvis_memory_core
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
